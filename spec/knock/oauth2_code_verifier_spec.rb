RSpec.describe Knock::Oauth2CodeVerifier do
  it "has a version number" do
    expect(Knock::Oauth2CodeVerifier::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
